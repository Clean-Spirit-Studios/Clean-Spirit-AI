<?xml version="1.0" encoding="utf-8"?>
<!--
  Network: used ONLY for the one-time model download on first launch.
  After download, the app runs 100% offline. No analytics, no telemetry.

  Storage permissions: needed by image_picker (gallery/camera) and
  file_picker (documents). Android 13+ uses granular READ_MEDIA_* permissions.
  Android < 13 falls back to READ_EXTERNAL_STORAGE (maxSdkVersion="32").
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network - for one-time model download only -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera - for image attachment via camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Android 13+ granular media permissions (image_picker) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Android < 13 storage permission (image_picker + file_picker fallback) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Keep screen on during long model downloads / generation -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="Clean Spirit AI"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:extractNativeLibs="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
